{% extends 'base.html.twig' %}

{% block title %}Veliko - My Account!{% endblock %}

{% block body %}

    <div style="display: flex">
        {% include 'partials/sidebar/sidebar.html.twig' %}
        <div>
            {% if stations %}
                {% for station in stations %}
                    {{ station.name }}
                {% endfor %}
            {% else %}
                <p>Tu n'as aucune stations en favories</p>
            {% endif %}
        </div>
    </div>

{% endblock %}
